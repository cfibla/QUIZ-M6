<form method="get" action="/quizes">
	<label for="search" class="rem"></label>
	<input type="text" name="search" value="Buscar pregunta" onFocus="this.value=''"/>
	<button type="submit">Enviar</button>
</form><br><p>

<table>
	<% var i; for (i=0; i < quizes.length; i++){ %>
		<tr>
			<td>
				<a href="quizes/<%= quizes[i].id %>"><%= quizes[i].tema %></a>
			</td>
			<td>
				<a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a>
			</td>
			<% if (session.user){%><!--Solo pueden verlo usuarios LOGIN-->
				<td>
					<a href="quizes/<%= quizes[i].id %>/edit"><button>editar</button></a>
				</td>
				<td>
					<form method='post' action='quizes/<%= quizes[i].id %>?_method=delete'>
						<button type='submit' onClick='return confirm("Borrar: <%= quizes[i].pregunta %>");'>borrar</button>
					</form>
			<%}%>
		</tr>
		<% } %>
</table><br><p>
<%if (session.user) { %>
	<a href = "/quizes/new"><button type"button">Crear pregunta</button></a>
<%}%>